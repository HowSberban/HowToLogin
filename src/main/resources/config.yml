# HTLogin 配置文件

# 登录/注册超时时间（秒），0 = 不限制
login-timeout: 60

# 会话超时时间（秒），0 = 无超时，每次重连都需要重新登录
session-timeout: 300

# 密码长度限制
min-password-length: 4
max-password-length: 32

# 超时未登录是否踢出玩家
kick-on-timeout: true

# 登录前是否禁止聊天
prevent-chat-before-login: true

# 消息文本请在 translations.properties 中修改